import React from 'react';
import './Navbar.scss';

const Navbar = () => (
  <header className="app__navbar">
    <h2 data-testid="logo" className="app__navbar--logo">
      PORT SEARCH APP
    </h2>
  </header>
);

export default Navbar;
